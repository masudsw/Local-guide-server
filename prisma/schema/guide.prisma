model GuideProfile {
  id         String   @id @default(uuid())
  userId     String   @unique
  expertise  String[]
  dailyRate  Float?
  reviews    Review[]
  isVerified Boolean
  user       User     @relation(fields: [userId], references: [id])
}
